# Public placeholder template only. The foundation needs no environment variables.
# Copy to .env.local only when a feature requires local configuration.
#
# VITE_* values are exposed to browser users, including in production bundles.
# Never put API keys, access tokens, account identifiers, or personal data in them.
# Future server-side credentials belong in a server environment or secret manager.
